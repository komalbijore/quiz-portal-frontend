import { Select } from "@mui/material";
import React from "react";

const QuizLevel = () => {
  const quizLevelOptions = [
    { label: "Easy", value: "easy" },
    { label: "Medium", value: "Medium" },
    { label: "Hard", value: "hard" },
  ];
  return <div>Quiz Level</div>;
};

export default QuizLevel;
